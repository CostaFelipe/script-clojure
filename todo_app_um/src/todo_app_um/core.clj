(ns todo-app-um.core)

(defonce todos (atom []))

(defn add-todo []
  nil)

(defn get-todos []
  nil)

(defn delete-todo []
  nil)

  (defn soma [x y]
    (+ x y))

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))
